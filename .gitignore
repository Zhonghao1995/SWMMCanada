# Secrets / credentials — NEVER commit (this repo will go public)
.env
.env.*
# .env.example is committed — it documents required vars and holds no secrets:
!.env.example
*.key
*.pem
secrets.*
gpt_config.json
credentials.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Node / web frontend
node_modules/
dist/
.next/
.vite/

# PyInstaller / build output — ROOT ONLY (must NOT match the swmmcanada/build package
# or tests/build dir, which are real source).
/build/
frontend/build/
backend/artifacts/

# Large geo / data artifacts (track sources, not bulk downloads)
data/raw/
data/cache/
backend/cache/
*.sqlite3
*.tif
*.tiff
*.gpkg

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local planning (PRDs, scratch) — not for the public repo
.claude/

# Internal design docs (ADRs, specs, design rationale, context) — kept local only
docs/
/CONTEXT.md
/DESIGN.md

# Root agent config (setup-matt-pocock-skills) — kept local, like docs/ above.
# (docs/agents/ is already covered by the docs/ rule above.)
/AGENTS.md
/CLAUDE.md
